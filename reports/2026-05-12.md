# 今日开发报告 - 2026-05-12 00:00:00
## Commit 记录
今日共1次
commit信息为:
- `92f0a18` 2026-05-12 Your Name — init: 迁移learning_system为独立仓库

## 代码改动

```diff
commit 92f0a18b79c0599fc3fc010b71d5fdeb33f2054e
Author: Your Name <you@example.com>
Date:   Tue May 12 06:26:26 2026 +0000

    init: 迁移learning_system为独立仓库

diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..dac9497
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,3 @@
+__pycache__/
+*.pyc
+*.env
\ No newline at end of file
diff --git a/reports/2026-05-12.md b/reports/2026-05-12.md
new file mode 100644
index 0000000..12ebced
--- /dev/null
+++ b/reports/2026-05-12.md
@@ -0,0 +1,356 @@
+# 今日开发报告 - 2026-05-12 00:00:00
+## Commit 记录
+今日共1次
+commit信息为:
+- `a14db15` 2026-05-12 Your Name — 修改main函数markdown存储位置，简单修改markdown写入内容
+
+## 代码改动
+
+```diff
+commit a14db15e39b2364c57540026e2bf99a2573854dd
+Author: Your Name <you@example.com>
+Date:   Tue May 12 02:30:07 2026 +0000
+
+    修改main函数markdown存储位置，简单修改markdown写入内容
+
+diff --git a/2026-05-11.md b/2026-05-11.md
+deleted file mode 100644
+index d6cf0fb..0000000
+--- a/2026-05-11.md
++++ /dev/null
+@@ -1,250 +0,0 @@
+-# 今日开发报告 - 2026-05-10 00:00:00
+-## Commit 记录
+-今日共4次
+-commit信息为:
+-- `6eefa77` 2026-05-11 Your Name — chore: 添加gitignore忽略pycache
+-- `2e8d489` 2026-05-11 Your Name — feat: 完成diff函数
+-- `73eed15` 2026-05-10 Your Name — feat: 添加git log功能
+-- `ae29f9f` 2026-05-10 muzi — feat: 完成路径检查和Git仓库判断功能
+-
+-f"## 代码改动
+-
+-```diff
+-commit 6eefa7738faacf1622f01c4e6c791953c991c30a
+-Author: Your Name <you@example.com>
+-Date:   Mon May 11 06:07:50 2026 +0000
+-
+-    chore: 添加gitignore忽略pycache
+-
+-diff --git a/.gitignore b/.gitignore
+-new file mode 100644
+-index 0000000..7a60b85
+---- /dev/null
+-+++ b/.gitignore
+-@@ -0,0 +1,2 @@
+-+__pycache__/
+-+*.pyc
+-diff --git a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc
+-deleted file mode 100644
+-index 49322c6..0000000
+-Binary files a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc and /dev/null differ
+-
+-commit 2e8d489530ce57918ac89e77161ec3d7eb766bbf
+-Author: Your Name <you@example.com>
+-Date:   Mon May 11 05:58:59 2026 +0000
+-
+-    feat: 完成diff函数
+-
+-diff --git a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc
+-index 9a0a098..49322c6 100644
+-Binary files a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc and b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc differ
+-diff --git a/learning_system/tool/git_daily_report/git_reader.py b/learning_system/tool/git_daily_report/git_reader.py
+-index 986865a..0f23a69 100644
+---- a/learning_system/tool/git_daily_report/git_reader.py
+-+++ b/learning_system/tool/git_daily_report/git_reader.py
+-@@ -1,9 +1,9 @@
+- from pathlib import Path
+- import subprocess
+- from datetime import datetime
+--path = "/home/muzi/python_project"
+--
+- 
+-+#临时全局测试时间变量
+-+testtime = "2026-05-10 00:00:00"
+- #检查路径是否存在或为文件夹
+- def check_path(text:str)->Path:
+-         text = Path(text)
+-@@ -18,10 +18,19 @@ def check_path(text:str)->Path:
+- def check_git(path:Path):
+-     return (path / ".git").is_dir()
+-          
+--    
+-+
+-+
+-+#获取当日简单日志
+- def git_log(path:Path):
+--    #当前时间
+--    today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+--    result = subprocess.run(["git","log","--oneline",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+-+    #当前时间变量
+-+    #today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+-+    result = subprocess.run(["git","log","--oneline",f"--since={testtime}"],cwd=path,capture_output=True,text=True)
+-+    return result.stdout
+-+
+-+
+-+#获取当日全部commint信息
+-+def git_diff(path:Path):
+-+    #today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+-+    result = subprocess.run(["git","log","-p",f"--since={testtime}"],cwd=path,capture_output=True,text=True)
+-     return result.stdout
+- 
+-diff --git a/learning_system/tool/git_daily_report/main.py b/learning_system/tool/git_daily_report/main.py
+-index c08035b..3311d1b 100644
+---- a/learning_system/tool/git_daily_report/main.py
+-+++ b/learning_system/tool/git_daily_report/main.py
+-@@ -1,12 +1,14 @@
+--from git_reader import check_git,check_path,git_log
+-+import git_reader
+- 
+- 
+- try:
+--    file_path = check_path(input("请输入路径："))
+-+    file_path = git_reader.check_path(input("请输入路径："))
+-     
+--    if check_git(file_path):
+-+    if git_reader.check_git(file_path):
+-         print("此路径是git仓库\n即将执行检查日志操作------")
+--        print(git_log(file_path))
+-+        print(git_reader.git_log(file_path))
+-+        print("---------\n准备执行diff")
+-+        print(git_reader.git_diff(file_path))
+-     else:
+-         print("此路径不为git仓库")
+- except FileNotFoundError:
+-
+-commit 73eed15c681d9d6417f2bf751902b735edd190be
+-Author: Your Name <you@example.com>
+-Date:   Sun May 10 09:04:24 2026 +0000
+-
+-    feat: 添加git log功能
+-
+-diff --git a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc
+-index b01a8c0..9a0a098 100644
+-Binary files a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc and b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc differ
+-diff --git a/learning_system/tool/git_daily_report/git_reader.py b/learning_system/tool/git_daily_report/git_reader.py
+-index 92b1db6..986865a 100644
+---- a/learning_system/tool/git_daily_report/git_reader.py
+-+++ b/learning_system/tool/git_daily_report/git_reader.py
+-@@ -1,4 +1,8 @@
+- from pathlib import Path
+-+import subprocess
+-+from datetime import datetime
+-+path = "/home/muzi/python_project"
+-+
+- 
+- #检查路径是否存在或为文件夹
+- def check_path(text:str)->Path:
+-@@ -15,7 +19,9 @@ def check_git(path:Path):
+-     return (path / ".git").is_dir()
+-          
+-     
+--    
+--
+--
+-+def git_log(path:Path):
+-+    #当前时间
+-+    today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+-+    result = subprocess.run(["git","log","--oneline",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+-+    return result.stdout
+- 
+-diff --git a/learning_system/tool/git_daily_report/main.py b/learning_system/tool/git_daily_report/main.py
+-index 6fea44a..c08035b 100644
+---- a/learning_system/tool/git_daily_report/main.py
+-+++ b/learning_system/tool/git_daily_report/main.py
+-@@ -1,11 +1,12 @@
+--from git_reader import check_git,check_path
+-+from git_reader import check_git,check_path,git_log
+- 
+- 
+- try:
+--    file_path = input("请输入路径：")
+-+    file_path = check_path(input("请输入路径："))
+-     
+--    if check_git(check_path(file_path)):
+--        print("此路径为git仓库")
+-+    if check_git(file_path):
+-+        print("此路径是git仓库\n即将执行检查日志操作------")
+-+        print(git_log(file_path))
+-     else:
+-         print("此路径不为git仓库")
+- except FileNotFoundError:
+-diff --git a/text.py b/text.py
+-index f67baf2..df22f5c 100644
+---- a/text.py
+-+++ b/text.py
+-@@ -1,2 +1,7 @@
+--a = ['1']
+--print(len(a))
+-+from datetime import date
+-+import subprocess
+-+
+-+today_time = date.today()
+-+subprocess.run(["git","log","--oneline",f"--since={today_time}"])
+-+
+-+
+-
+-commit ae29f9fbf3b17267d14e638f84775e658a245ee5
+-Author: muzi <muzijin2246@gmail.com>
+-Date:   Sun May 10 01:50:23 2026 +0000
+-
+-    feat: 完成路径检查和Git仓库判断功能
+-
+-diff --git a/aaa.git b/aaa.git
+-new file mode 100644
+-index 0000000..e69de29
+-diff --git a/learning_system/tool/git_daily_report/README.md b/learning_system/tool/git_daily_report/README.md
+-new file mode 100644
+-index 0000000..e69de29
+-diff --git a/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc
+-new file mode 100644
+-index 0000000..b01a8c0
+-Binary files /dev/null and b/learning_system/tool/git_daily_report/__pycache__/git_reader.cpython-312.pyc differ
+-diff --git a/learning_system/tool/git_daily_report/git_reader.py b/learning_system/tool/git_daily_report/git_reader.py
+-new file mode 100644
+-index 0000000..92b1db6
+---- /dev/null
+-+++ b/learning_system/tool/git_daily_report/git_reader.py
+-@@ -0,0 +1,21 @@
+-+from pathlib import Path
+-+
+-+#检查路径是否存在或为文件夹
+-+def check_path(text:str)->Path:
+-+        text = Path(text)
+-+        if not text.exists():
+-+            raise FileNotFoundError(f"路径不存在，{text}")
+-+        if not text.is_dir():
+-+            raise NotADirectoryError(f"此路径不是文件夹路径，{text}")
+-+        
+-+        return text
+-+        
+-+#检查路径是否为git仓库
+-+def check_git(path:Path):
+-+    return (path / ".git").is_dir()
+-+         
+-+    
+-+    
+-+
+-+
+-+
+-diff --git a/learning_system/tool/git_daily_report/main.py b/learning_system/tool/git_daily_report/main.py
+-new file mode 100644
+-index 0000000..6fea44a
+---- /dev/null
+-+++ b/learning_system/tool/git_daily_report/main.py
+-@@ -0,0 +1,16 @@
+-+from git_reader import check_git,check_path
+-+
+-+
+-+try:
+-+    file_path = input("请输入路径：")
+-+    
+-+    if check_git(check_path(file_path)):
+-+        print("此路径为git仓库")
+-+    else:
+-+        print("此路径不为git仓库")
+-+except FileNotFoundError:
+-+    print("这个路径不存在！")
+-+except NotADirectoryError:
+-+    print("这个路径不是一个文件夹！")
+-+
+-+
+-diff --git a/text.py b/text.py
+-new file mode 100644
+-index 0000000..f67baf2
+---- /dev/null
+-+++ b/text.py
+-@@ -0,0 +1,2 @@
+-+a = ['1']
+-+print(len(a))
+-
+-```"
+diff --git a/learning_system/reports/2026-05-12.md b/learning_system/reports/2026-05-12.md
+new file mode 100644
+index 0000000..a9ea562
+--- /dev/null
++++ b/learning_system/reports/2026-05-12.md
+@@ -0,0 +1,10 @@
++# 今日开发报告 - 2026-05-12 00:00:00
++## Commit 记录
++今日共0次
++commit信息为:
++
++## 代码改动
++
++```diff
++
++```
+diff --git a/learning_system/tool/git_daily_report/git_reader.py b/learning_system/tool/git_daily_report/git_reader.py
+index 5632304..4b9b4be 100644
+--- a/learning_system/tool/git_daily_report/git_reader.py
++++ b/learning_system/tool/git_daily_report/git_reader.py
+@@ -2,8 +2,9 @@ from pathlib import Path
+ import subprocess
+ from datetime import datetime
+ 
+-#临时全局测试时间变量
+-testtime = "2026-05-10 00:00:00"
++#设置全局当日时间变量
++today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
++
+ #检查路径是否存在或为文件夹
+ def check_path(text:str)->Path:
+         text = Path(text)
+@@ -22,16 +23,13 @@ def check_git(path:Path):
+ 
+ #获取当日简单日志
+ def git_log(path:Path):
+-    #当前时间变量
+-    #today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+-    result = subprocess.run(["git","log","--oneline","--format=%h|%an|%ad|%s","--date=short",f"--since={testtime}"],cwd=path,capture_output=True,text=True)
++    result = subprocess.run(["git","log","--oneline","--format=%h|%an|%ad|%s","--date=short",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+     return result.stdout
+ 
+ 
+ #获取当日全部commint信息
+ def git_diff(path:Path):
+-    #today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+-    result = subprocess.run(["git","log","-p",f"--since={testtime}"],cwd=path,capture_output=True,text=True)
++    result = subprocess.run(["git","log","-p",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+     return result.stdout
+ 
+     
+@@ -46,12 +44,12 @@ def generate_report(path:Path):
+     for a in log.splitlines():
+          parts = a.split("|")
+          commit_lines += f"- `{parts[0]}` {parts[2]} {parts[1]} — {parts[3]}\n"
+-    return f"""# 今日开发报告 - {testtime}
++    return f"""# 今日开发报告 - {today_time}
+ ## Commit 记录
+ 今日共{markdown['commitnumber']}次
+ commit信息为:
+ {commit_lines}
+-f"## 代码改动\n\n```diff\n{markdown['commitdiff']}\n```"
++## 代码改动\n\n```diff\n{markdown['commitdiff']}\n```
+ """
+ 
+     
+diff --git a/learning_system/tool/git_daily_report/main.py b/learning_system/tool/git_daily_report/main.py
+index 29a3aef..ccef3c1 100644
+--- a/learning_system/tool/git_daily_report/main.py
++++ b/learning_system/tool/git_daily_report/main.py
+@@ -11,8 +11,10 @@ try:
+         print("完成日志检查---------\n准备执行diff查询区别")
+         print(git_reader.git_diff(file_path))
+         print("完成diff查询---------\n准备创建markdown文件并写入日志")
+-        Path(file_path/f"{datetime.now().strftime("%Y-%m-%d")}.md").write_text(git_reader.generate_report(file_path))
+-        print(f"已添加{file_path/f"{datetime.now().strftime("%Y-%m-%d")}"}git日志文件")
++        reports_dir = file_path/Path("learning_system/reports")
++        reports_dir.mkdir(exist_ok=True)
++        Path(reports_dir/f"{datetime.now().strftime("%Y-%m-%d")}.md").write_text(git_reader.generate_report(file_path))
++        print(f"已添加{reports_dir/f"{datetime.now().strftime("%Y-%m-%d")}.md"}格式的git日志记录")
+     else:
+         print("此路径不为git仓库")
+ except FileNotFoundError:
+
+```
diff --git "a/reports/2026-05-12deepseek\345\210\206\346\236\220.txt" "b/reports/2026-05-12deepseek\345\210\206\346\236\220.txt"
new file mode 100644
index 0000000..3355d8c
--- /dev/null
+++ "b/reports/2026-05-12deepseek\345\210\206\346\236\220.txt"
@@ -0,0 +1,12 @@
+根据提交记录，本次变更主要涉及 **每日报告 Markdown 文件的存储路径调整与内容写入逻辑优化**。具体分析如下：
+
+### 1. 文件位置迁移
+- **删除** 根目录下的 `2026-05-11.md`（250 行内容移除），不再在项目根目录生成日报文件。
+- **新增** `learning_system/reports/2026-05-12.md`（新增 10 行），表明报告现在生成到 `learning_system/reports/` 子目录下，按日期命名。
+
+### 2. 代码逻辑调整
+- **git_reader.py**（16 行变更）：推测修改了生成 Markdown 内容或文件路径的函数，可能将保存路径从根目录改为 `reports/` 目录，或调整了内容格式。
+- **main.py**（6 行变更）：主函数中调用报告生成的部分发生改动，很可能是更新了目标路径参数或简化了写入逻辑（如“简单修改 markdown 写入内容”）。
+
+### 总结
+这是一次 **目录结构整理 + 轻微功能调整** 的提交：将每日报告集中到 `learning_system/reports` 目录下管理，并相应调整工具代码，使生成路径和内容更规范。
\ No newline at end of file
diff --git a/tool/git_daily_report/README.md b/tool/git_daily_report/README.md
new file mode 100644
index 0000000..e69de29
diff --git a/tool/git_daily_report/git_reader.py b/tool/git_daily_report/git_reader.py
new file mode 100644
index 0000000..e1cdd0a
--- /dev/null
+++ b/tool/git_daily_report/git_reader.py
@@ -0,0 +1,85 @@
+from pathlib import Path
+from datetime import datetime
+from openai import OpenAI
+from dotenv import load_dotenv
+import subprocess
+import os
+#导入.env环境变量
+load_dotenv()
+
+#设置全局当日时间变量
+today_time = datetime.now().strftime("%Y-%m-%d 00:00:00")
+
+#检查路径是否存在或为文件夹
+def check_path(text:str)->Path:
+        text = Path(text)
+        if not text.exists():
+            raise FileNotFoundError(f"路径不存在，{text}")
+        if not text.is_dir():
+            raise NotADirectoryError(f"此路径不是文件夹路径，{text}")
+        
+        return text
+        
+#检查路径是否为git仓库
+def check_git(path:Path):
+    return (path / ".git").is_dir()
+         
+
+
+#获取当日简单日志
+def git_log(path:Path):
+    result = subprocess.run(["git","log","--oneline","--format=%h|%an|%ad|%s","--date=short",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+    return result.stdout
+
+
+#获取当日全部commint信息
+def git_diff(path:Path):
+    result = subprocess.run(["git","log","-p",f"--since={today_time}"],cwd=path,capture_output=True,text=True)
+    return result.stdout
+
+def git_stat(path:Path):
+     result = subprocess.run(["git", "log", "--stat", f"--since={today_time}", "--format=%h %s"],cwd=path,capture_output=True,text=True)
+     return result.stdout
+
+#获取今日开发报告并保存为markdown文件
+def generate_report(path:Path):
+    markdown = {}
+    log = git_log(path)
+    diff = git_diff(path)
+    markdown["commitnumber"] = len(log.splitlines())
+    markdown["commitinfo"] = log
+    markdown["commitdiff"] = diff
+    commit_lines = ""
+    for a in log.splitlines():
+         parts = a.split("|")
+         commit_lines += f"- `{parts[0]}` {parts[2]} {parts[1]} — {parts[3]}\n"
+    return f"""# 今日开发报告 - {today_time}
+## Commit 记录
+今日共{markdown['commitnumber']}次
+commit信息为:
+{commit_lines}
+## 代码改动\n\n```diff\n{markdown['commitdiff']}\n```
+"""
+
+#调用deepseek任命为"负责分析我提供给你的git记录"
+def deepseekapi(stat:str):
+    client = OpenAI(
+    api_key=os.getenv('deepseekapikey'),
+    base_url="https://api.deepseek.com")
+
+    response = client.chat.completions.create(
+        model="deepseek-v4-pro",
+        messages=[
+            {"role": "system", "content": "你负责分析我提供给你的git记录"},
+            {"role": "user", "content": f"以下是今日git记录\n{stat}\n帮我简单分析变更信息"},
+        ],
+        stream=False,
+        reasoning_effort="high",
+        extra_body={"thinking": {"type": "enabled"}}
+    )
+    return response.choices[0].message.content
+
+
+
+    
+    
diff --git a/tool/git_daily_report/main.py b/tool/git_daily_report/main.py
new file mode 100644
index 0000000..420db9c
--- /dev/null
+++ b/tool/git_daily_report/main.py
@@ -0,0 +1,29 @@
+import git_reader
+from pathlib import Path
+from datetime import datetime
+
+try:
+    file_path = git_reader.check_path(input("请输入路径："))
+    
+    if git_reader.check_git(file_path):
+        print("此路径是git仓库\n即将执行检查日志操作------")
+        print(git_reader.git_log(file_path))
+        print("完成日志检查---------\n准备执行diff查询区别")
+        print(git_reader.git_diff(file_path))
+        print("完成diff查询---------\n准备创建markdown文件并写入日志")
+        reports_dir = file_path/Path("learning_system/reports")
+        reports_dir.mkdir(exist_ok=True)
+        Path(reports_dir/f"{datetime.now().strftime("%Y-%m-%d")}.md").write_text(git_reader.generate_report(file_path))
+        print(f"已添加{reports_dir/f"{datetime.now().strftime("%Y-%m-%d")}.md"}格式的git日志记录")
+        print("---开始调用deepseek整理日志变化信息！请等待......---")
+        deepseek_report = git_reader.deepseekapi(git_reader.git_stat(file_path))
+        Path(reports_dir/f"{datetime.now().strftime("%Y-%m-%d")}deepseek分析.txt").write_text(deepseek_report)
+        print(deepseek_report)
+    else:
+        print("此路径不为git仓库")
+except FileNotFoundError:
+    print("这个路径不存在！")
+except NotADirectoryError:
+    print("这个路径不是一个文件夹！")
+
+

```
